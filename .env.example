# Copy to .env and fill in. Compose reads this file for ${LASTFM_API_KEY}.
LASTFM_API_KEY=

# Only needed to run the app outside Docker (uvicorn on the host). Under
# compose, docker-compose.yml sets DATABASE_URL to the "db" service instead.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/lastfm
