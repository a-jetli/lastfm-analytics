# Pinned so a rebuild months from now installs the versions this was tested
# against, and so CI can't go red from an upstream release with no commit
# behind it. Bump deliberately, not by accident.
fastapi==0.136.3
uvicorn[standard]==0.44.0
requests==2.34.2
psycopg[binary]==3.3.4
python-dotenv==1.2.2
